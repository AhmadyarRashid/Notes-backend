/**
 * This module file is used to create instances of all services and simplify imports in to our controllers
 */

import { SystemService } from './system.services'
export const systemService = new SystemService();