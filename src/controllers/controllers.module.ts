/**
* This module file is used to create instances of all controllers and simplify imports in to our routers
*/

import { SystemController } from './system.controller'
export const systemController = new SystemController();